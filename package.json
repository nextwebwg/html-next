{
  "name": "html-next-workspace",
  "version": "0.0.0",
  "private": true,
  "description": "Reference implementations for HTML Next proposals.",
  "license": "MIT",
  "type": "module",
  "packageManager": "pnpm@11.25.0",
  "engines": {
    "node": ">=22.13 <23 || >=24 <25"
  },
  "scripts": {
    "audit:native": "pnpm --filter @nextwebwg/html-next audit:native",
    "build": "pnpm --recursive --if-present build",
    "build:example": "pnpm --filter @nextwebwg/html-next build:example",
    "check:generated": "pnpm --filter @nextwebwg/html-next check:generated",
    "clean": "pnpm --recursive --if-present run clean",
    "generate:dom": "pnpm --filter @nextwebwg/html-next generate:dom",
    "generate:snapshots": "pnpm --filter @nextwebwg/html-next generate:snapshots",
    "lint": "oxlint --config oxlint.json --deny-warnings .",
    "measure:runtime": "pnpm --filter @nextwebwg/html-next measure:runtime",
    "measure:hydration": "pnpm --filter @nextwebwg/html-next measure:hydration",
    "measure:performance": "pnpm --filter @nextwebwg/html-next measure:performance",
    "measure:reactivity": "pnpm --filter @nextwebwg/html-next measure:reactivity",
    "report:performance": "pnpm --filter @nextwebwg/html-next report:performance",
    "test": "vitest run --config vitest.config.ts --exclude tests/package.test.ts",
    "test:browser": "vitest run --config vitest.browser.config.ts",
    "test:consumer": "vitest run --config vitest.consumer.config.ts",
    "test:package": "vitest run --config vitest.config.ts tests/package.test.ts",
    "test:targets": "vitest run --config vitest.targets.config.ts",
    "typecheck": "tsc -p tsconfig.json && pnpm --recursive --if-present typecheck",
    "verify:inner": "pnpm lint && pnpm typecheck && pnpm test && pnpm check:generated && pnpm measure:runtime",
    "verify:pr": "pnpm verify:inner && pnpm build && pnpm test:package",
    "verify:compatibility": "pnpm test:browser && pnpm test:targets && pnpm test:consumer",
    "verify:extended": "pnpm verify:pr && pnpm verify:compatibility",
    "verify:release": "pnpm --filter @nextwebwg/html-next check:release && pnpm test:consumer",
    "release:dry-run": "pnpm --filter @nextwebwg/html-next release:dry-run"
  },
  "devDependencies": {
    "@types/node": "catalog:",
    "oxlint": "catalog:",
    "tsx": "catalog:",
    "typescript": "catalog:",
    "vitest": "catalog:"
  }
}
