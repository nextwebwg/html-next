# `@nextwebwg/declarative-components`

Reference implementation for the [Declarative HTML Components proposal](https://nextwebwg.org/html-next/). It provides the browser runtime, component parser, validator, and framework-target generator.

The package is experimental and is not published yet. From the repository root:

```sh
corepack pnpm build
corepack pnpm test
```
